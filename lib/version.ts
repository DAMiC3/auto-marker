// Human-facing release number — bump this by hand on every meaningful release so
// customers (and you) can see it in Settings. For verifying a specific deploy is
// actually live, see the auto-generated commit/timestamp in lib/buildInfo.ts instead.
export const APP_VERSION = "1.3";
